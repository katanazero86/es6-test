{
  "name": "es6-test",
  "version": "1.0.0",
  "description": "es6 test 과제 코드",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -c",
    "start": "node ./build/js/main.min.js",
    "build run" : "npm run build && npm run start"
  },
  "author": "kyle",
  "license": "ISC",
  "dependencies": {
    "rollup": "^0.68.2"
  }
}
